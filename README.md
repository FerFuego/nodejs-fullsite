# nodejs-fullsite

Dependencies:
  - express
  - pug (motor de plantillas)
  - sequelize
  - nodemon (demonio que actualiza el servidor, para no reiniciarlo a cada rato)
  - mysql (base de datos)


console: npm run dev